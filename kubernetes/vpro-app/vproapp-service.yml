apiVersion: v1
kind: Service
metadata:
  name: vproapp-service
spec:
  ports:
  - port: 80
    targetPort: vproapp-port
    nodePort: 30002
    protocol: TCP
  selector:
    app: vproapp
  type: NodePort
